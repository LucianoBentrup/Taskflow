// Script de seed do banco de dados.
// Os seeds reais serão adicionados junto com os models (Sprint 1+).
async function main() {
  // TODO: implementar seeds quando os models forem criados.
}

main().catch((error) => {
  console.error(error);
  process.exit(1);
});
